import styled from 'styled-components';
import { Theme } from '../../../themes/Theme';

const StyledCreatePersonSalaryEditor = styled.div<{ theme: Theme }>``;

export { StyledCreatePersonSalaryEditor };
