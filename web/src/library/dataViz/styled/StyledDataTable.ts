import styled from 'styled-components';
import { Theme } from '../../../themes/Theme';
import { Box } from '@mui/material';

const StyledDataTable = styled(Box)<{ theme: Theme }>``;

export { StyledDataTable };
